ALTER TABLE GENRE ADD MOVIE_ID INTEGER;
ALTER TABLE GENRE ADD FOREIGN KEY (MOVIE_ID) REFERENCES MOVIE(id);
UPDATE GENRE SET MOVIE_ID = (SELECT MOVIE_ID FROM MOVIE_GENRE g where g.GENRE_ID = ID);
DROP TABLE MOVIE_GENRE;
ALTER TABLE GENRE ADD FIT INTEGER;
ALTER TABLE MOVIE ADD (
    FAMILY_FIT INTEGER, FRIENDS_FALTER TABLE GENRE ADD MOVIE_ID INTEGER;
                                 ALTER TABLE GENRE ADD FOREIGN KEY (MOVIE_ID) REFERENCES MOVIE(id);
                                 UPDATE GENRE SET MOVIE_ID = (SELECT MOVIE_ID FROM MOVIE_GENRE g where g.GENRE_ID = ID);
                                 DROP TABLE MOVIE_GENRE;
                                 ALTER TABLE GENRE ADD FIT INTEGER;
                                 ALTER TABLE MOVIE ADD (
                                     FAMILY_FIT INTEGER, FRIENDS_FIT INTEGER, ROMANTIC_FIT INTEGER,
                                     LOW_CONCENTRATION_FIT INTEGER, MODERATE_CONCENTRATION_FIT INTEGER, HARD_CONCENTRATION_FIT INTEGER
                                     );
                                 CREATE TABLE KEYWORD (ID IDENTITY, MOVIE_ID INTEGER, FIT INTEGER, KEYWORD VARCHAR(500), FOREIGN KEY (MOVIE_ID) REFERENCES MOVIE(ID));
IT INTEGER, ROMANTIC_FIT INTEGER,
    LOW_CONCENTRATION_FIT INTEGER, MODERATE_CONCENTRATION_FIT INTEGER, HARD_CONCENTRATION_FIT INTEGER
    );
CREATE TABLE KEYWORD (ID IDENTITY, MOVIE_ID INTEGER, FIT INTEGER, KEYWORD VARCHAR(500), FOREIGN KEY (MOVIE_ID) REFERENCES MOVIE(ID));
CREATE TABLE MOVIE_LANGUAGES (movie_id BIGINT, language VARCHAR(50));
CREATE TABLE MOVIE_Platform (movie_id BIGINT, platform VARCHAR(50));
ALTER TABLE MOVIE ADD AGE_RESTRICTION VARCHAR(50);
ALTER TABLE MOVIE ADD MOVIE_PICTURE BLOB;